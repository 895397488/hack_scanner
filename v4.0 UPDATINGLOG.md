# 更新日志

All notable changes to Hack Scanner will be documented in this file.

---

## [v5.0] — 2026-06-27 — 业务逻辑与影响范围增强

### 新增检测模块（3个）

| # | 模块 | 检测能力 | CWE | CVSS |
|---|------|----------|-----|------|
| 12 | **WeakPasswordCracker** | 弱口令字典爆破、用户枚举、无验证码/速率限制登录端点检测 | CWE-307/CWE-521 | 8.5-9.8 |
| 13 | **BusinessLogicChecker** | 价格篡改（负数/零元购）、数量溢出、积分/优惠券重放、金额URL参数注入 | CWE-840 | 7.5-9.5 |
| 14 | **ImpactEvaluator** | 敏感字段扫描、数据量级评估、越权影响范围自动升级 | CWE-200/CWE-359 | 动态 |

### 新增检测模块（前轮已合并）

| # | 模块 | 检测能力 | CWE | CVSS |
|---|------|----------|-----|------|
| 15 | **UnauthorizedAccessChecker** | 匿名敏感路径/API端点探测，200+路径覆盖 | CWE-306 | 8.1 |
| 16 | **AuthorizationBypassChecker** | IDOR越权（ID参数替换/路径数字ID遍历/数组数据对比） | CWE-639/CWE-284 | 8.1-8.6 |
| 17 | **CSRFTokenChecker** | CSRF token缺失、SameCookie策略、跨域凭证+POST组合风险 | CWE-352 | 4.3-8.1 |
| 18 | **PasswordResetVulnerabilityDetector** | 密码重置token强度、邮箱枚举、CSRF保护检查 | CWE-640/CWE-203 | 5.4-8.1 |

### config.json 新增开关（4个）

```jsonc
{
  "urls": {
    "check_weak_password": true,       // 弱口令/认证爆破检测
    "check_business_logic": true,      // 业务逻辑漏洞检测（支付/积分/促销）
    "check_impact_eval": true,         // 漏洞影响范围自动评估
    "check_authorization_bypass": true // 越权访问(IDOR)检测 — 前轮已添加
  }
}
```

### UnauthorizedAccessChecker 增强（SENSITIVE_PATHS +20条）

| 类别 | 新增路径示例 |
|------|-------------|
| 调试/信息泄露 | `phpinfo.php`、`/debug/vars`、`/profiler`、`/jolokia/list`、`/.well-known/security.txt` |
| SVN/Git 代码泄露 | `.git/config`、`.svn/entries`、`.hg/hgrc`、`.DS_Store`、`composer.lock`、`Gemfile.lock` |
| GitHub/GitLab 镜像 | `/raw.githubusercontent.com/`、`/api.github.com/repos/` |
| 敏感备份文件 | `backup/db.sql`、`vendor/composer/installed.json`、`Pipfile.lock` |

### ScanResult 新增字段

| 字段名 | 类型 | 用途 |
|--------|------|------|
| `impact_assessments` | `List[Dict]` | ImpactEvaluator 评估结果（数据量级/敏感字段/推荐升级） |

### URLScanner.scan() 扫描步骤扩展

| 编号 | 检测项 | 状态 |
|------|--------|------|
| 21.9 | 弱口令/认证爆破检测 | **新增** |
| 21.10 | 业务逻辑漏洞检测（支付/积分/促销） | **新增** |
| 21.11 | 漏洞影响范围自动评估 + 严重等级自动升级 | **新增** |

**扫描步骤总计：24 → 27 步**

---

## [v4.0] — 2026-06-25 — w3af 深度整合

从 [w3af](https://github.com/andresriancho/w3af) 提取核心检测逻辑，注入 hack 扫描器。
**零额外依赖**，所有新增功能仍只需 `requests + bs4 + tldextract`。

### 新增检测模块（7个）

| # | 模块 | w3af 来源 | 检测能力 | CWE | CVSS |
|---|------|-----------|----------|-----|------|
| 1 | **XXEDetector** | `audit/xxe.py` | XML外部实体注入 + JSON/XML混合攻击 | CWE-611 | 9.8 |
| 2 | **FileUploadDetector** | `audit/file_upload.py` | WebShell上传、GIF PHP Stub绕过 | CWE-434 | 10.0 |
| 3 | **DeserializationDetector** | `audit/deserialization.py` | Java/Pickle/PHP/.NET 不安全反序列化 | CWE-502 | 9.8 |
| 4 | **DVCSLeakDetector** | `crawl/find_dvcs.py` | .git/.svn/.hg/.bzr/.DS_Store + 备份目录泄露 | CWE-538 | 9.8 |
| 5 | **OpenAPIDiscovery** | `crawl/open_api.py` | Swagger/OpenAPI/SpringDoc文档自动发现(30+路径) | CWE-200 | 3.1 |
| 6 | **CMSFingerprint** + **CMSEnumDetector** | `crawl/wordpress_fingerprint.py` | 10种CMS识别 + 版本/用户枚举 | CWE-200/CWE-203 | 3.1/5.3 |
| 7 | **HTTPMethodsChecker** | `audit/dav.py` + `auth` | TRACE/XST/PUT写入/DELETE删除/OPTIONS信息泄露 | CWE-693/CWE-659 | 6.5-9.8 |

### 增强检测器（4个）

| # | 模块 | 增强内容 | w3af 参考 |
|---|------|----------|-----------|
| 8 | **SQLiBlindDetector** (新增类) | 精准时序盲注分析：5次基线采样 + 5次注入采样，差值>2.5s判定为高危 | `audit/blind_sqli.py` |
| 9 | **SSRFDetectorEnhanced** | URL重定向测试 (`http://127.0.0.1` / `//evil.com` / `\evil.com`) | SSRF增强 |
| 10 | **ExtendedHeaderChecker** (新增类) | COOP / CORP / COEP / Clear-Site-Data / Feature-Policy 5个新安全头 | `audit/ssl_certificate.py` |
| 11 | **SQLiDetector._infer_var_type()** | 补充参数类型推断方法，支持 int/path/url/email/string 五种类型 | — |

### config.json 新增开关（10个）

```jsonc
{
  "urls": {
    "check_xxe": true,           // XXE注入检测
    "check_file_upload": true,   // 文件上传漏洞检测
    "check_deserialization": true,// 反序列化漏洞检测
    "check_http_methods": true,   // HTTP方法安全检测
    "check_dvcs_leak": true,     // VCS泄露检测
    "check_openapi": true,       // OpenAPI发现
    "check_cms_fingerprint": true,// CMS指纹识别
    "check_sqli_blind": true,    // Blind Timing SQLi增强
    "check_ssrf_redirect": true,  // SSRF重定向测试
    "check_extended_headers": true// 扩展安全头检测
  }
}
```

### ScanResult 新增字段

| 字段名 | 类型 | 用途 |
|--------|------|------|
| `openapi_urls` | `List[Dict]` | 发现的OpenAPI文档URL及端点数 |
| `cms_fingerprints` | `List[str]` | CMS指纹识别结果（如 WordPress, Drupal） |
| `dvcs_leaks` | `List[Dict]` | VCS泄露详情 |
| `http_methods` | `Dict[str, Dict]` | HTTP方法检测结果映射 |

### ReportGenerator 变更

- HTML报告新增 **OpenAPI/Swagger文档表格**（URL + 端点数量）
- HTML报告新增 **CMS指纹标签区**（彩色标签展示）
- Footer 更新为 v2.0 标识

### URLScanner.scan() 扫描步骤扩展

| 原编号 | 新编号 | 检测项 | 状态 |
|--------|--------|--------|------|
| - | 10 | XXE注入检测 | **新增** |
| - | 11 | 文件上传漏洞检测 | **新增** |
| - | 12 | 反序列化漏洞检测 | **新增** |
| - | 13 | HTTP方法安全检测 | **新增** |
| - | 14 | VCS泄露检测 | **新增** |
| - | 15 | OpenAPI发现 | **新增** |
| - | 16 | CMS指纹识别 | **新增** |
| - | 17 | Blind Timing SQLi增强 | **新增** |
| - | 18 | SSRF重定向测试 | **新增** |
| - | 19 | 扩展安全头检测 | **新增** |
| 9 | 9 | RCE命令注入 | 保留原样 |
| 10 | 20 | 敏感信息泄露 | 保留原样 |
| 11 | 21 | SSL/TLS检查 | 保留原样 |
| 12 | 22 | HTTP安全头 | 保留原样 |
| 13 | 23 | CORS检查 | 保留原样 |
| - | 24 | SQL注入（原有） | 保留原样 + `_infer_var_type()` 补充 |

**扫描步骤总计：13 → 24 步**

### hack_scanner.py 变更

- epilog 更新，列出 w3af 整合后全部检测能力
- AI快速模式兼容新增检测开关

---

## 本次修复的 Bug

| Bug | 原因 | 修复方式 |
|-----|------|----------|
| `SQLiDetector._infer_var_type` AttributeError | 调用点引用但方法未定义 | 补充 `_infer_var_type()` 静态方法，实现5种类型推断 |
| `_test_url()` 参数数量不匹配 (7 vs 8) | 白盒增强时多传了 `param_name` 和 `context_test` 两个独立参数 | 移除多余的 `param_name`，只保留 `context_test` Dict |
| `XXEDetector.PAYLOADS` 解包错误 `(expected 2)` | PAYLOADS 是字符串列表，代码用 `for a, b in list` 元组解包 | 改为 `enumerate()` + 内联描述数组 |
| `FileUploadDetector.WEB_SHELL_PAYLOADS` vs `WEBSHELL_PAYLOADS` | 常量名定义和引用不一致（下划线 vs 无下划线） | 统一为 `WEBSHELL_PAYLOADS` |

---
